import { describe, it, beforeEach } from 'node:test'
import assert from 'node:assert'

describe('Message', () => {
  it('should throw on validate', () => assert.fail('todo'))
  it('should accept on validate', () => assert.fail('todo'))
})
